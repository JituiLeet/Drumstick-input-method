# Native build is performed by scripts/build-rime-native.sh because librime and
# its C++ dependencies are linked statically into liblibrime.so.
# This file is kept for Android Studio source navigation only.
cmake_minimum_required(VERSION 3.18.1)
project(librime_placeholder)
